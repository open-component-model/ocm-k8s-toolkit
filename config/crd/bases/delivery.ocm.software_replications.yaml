---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.0
  name: replications.delivery.ocm.software
spec:
  group: delivery.ocm.software
  names:
    kind: Replication
    listKind: ReplicationList
    plural: replications
    singular: replication
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Replication is the Schema for the replications API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ReplicationSpec defines the desired state of Replication
            properties:
              componentRef:
                description: ComponentRef is a reference to a Component to be replicated.
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                type: object
              interval:
                description: Interval at which the replication is reconciled.
                type: string
              suspend:
                description: |-
                  Suspend tells the controller to suspend the reconciliation of this
                  Replication.
                type: boolean
              targetRepositoryRef:
                description: targetRepositoryRef is a reference to an OCMRepository
                  the component to be replicated to.
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                type: object
            required:
            - componentRef
            - interval
            - targetRepositoryRef
            type: object
          status:
            description: ReplicationStatus defines the observed state of Replication
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
