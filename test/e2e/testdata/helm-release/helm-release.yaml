apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: helm-flux
  namespace: default
spec:
  interval: 30s
  chartRef:
    apiVersion: delivery.ocm.software/v1alpha1
    kind: Resource
    name: helm-resource
    namespace: default