apiVersion: v1alpha1
kind: LocalizationConfig
metadata:
  name: deployment-localization
spec:
  rules:
  - source:
      resource:
        name: image
    target:
      file:
        path: values.yaml
        value: deploy.image